// Help module
document.addEventListener("DOMContentLoaded", () => {
  const helpDiv = document.getElementById("help");
  if (helpDiv) {
    helpDiv.innerHTML = "<h2>Usage Guide</h2><p>Steps to run jobs, troubleshoot errors, and configure mappings.</p>";
  }
});